---
interface Props {
    title: string;
    subtitle: string;
}

const { title, subtitle } = Astro.props as Props;
---

<div class="flex flex-col items-center pt-10">
    <h1 class="text-5xl font-mono mb-4">{title}</h1>
    <h2 class="text-xl font-mono">{subtitle}</h2>
</div>
